# --- اجرای پایه ---
# ⚠️ حالت `python main.py --dry-run` به هیچ‌کدام از این‌ها نیاز ندارد؛
# Black-Scholes، IV، نوتیفایر تلگرام و SQLite همه با کتابخانه استاندارد کار می‌کنند.

# داده بازار بورس تهران (TSETMC)
pytse-client>=0.17.0

# ربات تلگرام (اختیاری: TelegramNotifier با urllib هم کار می‌کند)
python-telegram-bot>=21.0

# اعتبارسنجی/مدل‌سازی داده برای توسعه‌های بعدی
pydantic>=2.6

# تحلیل داده و محاسبات عددی
pandas>=2.2
numpy>=1.26

# توابع آماری برای قیمت‌گذاری (پیاده‌سازی فعلی fallback داخلی با math.erf دارد)
scipy>=1.12

# خواندن config/settings.yaml
PyYAML>=6.0

# --- کشف API پلتفرم کارگزاری (اختیاری) ---
# فقط برای scripts/emofid_login.py لازم است. مسیر جایگزین (HAR) هیچ نصبی نمی‌خواهد.
# اگر Chrome سیستم را دارید، به `playwright install` نیازی نیست.
# playwright>=1.44

# --- تست ---
pytest>=8.0
